# Vite env vars must be prefixed with VITE_
# Copy to kozak-website/.env and fill in as needed.

# Optional: show an Email button/link (if unset, Email is hidden)
VITE_CONTACT_EMAIL=

# Optional: show a Facebook button/link (if unset, Facebook is hidden)
VITE_FACEBOOK_URL=

# Optional but recommended: send contact form submissions to your email via Formspree.
# 1) Create a form at https://formspree.io
# 2) Copy your form ID (looks like: abcdefgh)
# 3) Set it here so submissions POST to https://formspree.io/f/<ID>
VITE_FORMSPREE_FORM_ID=
