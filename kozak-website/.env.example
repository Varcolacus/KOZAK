# Vite env vars must be prefixed with VITE_
# Copy to kozak-website/.env and fill in as needed.

# Optional: show an Email button/link (if unset, Email is hidden)
VITE_CONTACT_EMAIL=

# Optional: show a Facebook button/link (if unset, Facebook is hidden)
VITE_FACEBOOK_URL=
